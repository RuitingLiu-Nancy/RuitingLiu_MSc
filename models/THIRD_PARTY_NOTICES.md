# Third-party model notice

The two utility cross-encoder checkpoints were fine-tuned from the configured
`cross-encoder/ms-marco-MiniLM-L-6-v2` checkpoint. The canonical upstream model
page is <https://huggingface.co/cross-encoder/ms-marco-MiniLM-L6-v2> and marks
the upstream model as Apache-2.0. A copy of that licence is included in
`THIRD_PARTY_LICENSES/APACHE-2.0.txt`.

The Apache-2.0 text applies to the upstream MiniLM checkpoint.
